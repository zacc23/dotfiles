# aliases
 
alias am=alsamixer

alias bc='bc ~/.bcrc -l -q'

alias btc=bluetoothctl

alias cemu='~/git/CEmu/gui/qt/CEmu'

alias config='git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME'

alias doas='doas -- '

alias dt='dict -d wn'

alias fl=figlet

alias fp='ffplay -hide_banner'

alias gimp='gimp -s'

alias hc=HeadsetControl

alias la='ls -a'

alias ll='ls -l'

alias ls='ls --color'

alias fw='httpdirfs "https://open.audio" ~/.local/music'

alias make='make -j8'

alias mci='make clean install'

alias mls='mpv "$(ls --color=never -t | sed q)"'

alias mp=mupdf-x11

alias mpa='mpv --no-video'

alias nf=neofetch

alias pm=pulsemixer

alias sfu=sfeed_update

alias sl='streamlink -v'

alias spc=speedtest-cli

alias sx=startx

alias tdd='dict -d devil'

alias th='dict -d moby-thesaurus'

alias tmux='tmux -2'

alias tn=twitchnotifier

alias vi=vim

alias vis=cli-visualizer

alias wf='curl https://v2.wttr.in'

alias wr='curl "https://wttr.in/?format="%l:+%c+%C+%t""'

alias wtf=netbsd-wtf

alias xa=xbps-alternatives

alias xb='xrandr --output DVI-D-0 --brightness'

alias xi=xbps-install

alias xq=xbps-query

alias xr=xbps-remove

alias xrc=xbps-reconfigure

alias yd=youtube-dl

alias zt=zathura

# functions

function lst { ls -t | sed "$1"q };

function md { markdown "$1" | w3m -T text/html };

function sfp { sfeed_plain $HOME/.sfeed/feeds/"$1" > $HOME/.sfeed/"$1" };

function w3mimg { w3m -o imgdisplay=/usr/lib/w3m/w3mimgdisplay "$1" };

function w3mimgdisplay { /usr/lib/w3m/w3mimgdisplay <<<$'0;1;100;100;100;100;;;;;"$1"\n4;\n3;i' };
